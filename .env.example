# Database Configuration
# Copy this file to .env.local and update with your actual values

# Required: Database password (no default for security)
DATABASE_PASSWORD=

# Optional: Database connection settings (defaults provided)
DATABASE_HOST=dpg-d1can6re5dus73fcd83g-a.oregon-postgres.render.com
DATABASE_PORT=5432
DATABASE_NAME=possue2_db_v5
DATABASE_USERNAME=possue2_db_v5_user

# For local development, you may also need:
# DATABASE_URL=postgres://possue2_db_v5_user:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?sslmode=require

# SendGrid Email Configuration
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=info@possue.com
SENDGRID_TEST_EMAIL=info@possue.com

# Email Verification URLs
EMAIL_CONFIRMATION_URL=http://localhost:1337/api/auth/email-confirmation
EMAIL_CONFIRMATION_REDIRECT_URL=http://localhost:3000/email-confirmed

# Production environment variables are typically set in:
# - Render.com: Service > Settings > Environment Variables
# - Other hosting providers: Check their documentation
